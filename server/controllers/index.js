const home = require('./home-controller');
const users = require('./users-controller');
const product = require('./product-controller');
const order = require('./order-constroller');

module.exports = {
    home,
    users,
    product,
    order
};
